
function WelcomeBack() {


    return (
        <>
            <h1>tunganhle11</h1>


            {/* {(props.button && props.button.length > 0) ? props.button.map(item => (

                <div id="metu">
                    <div style={{ position: 'fixed', top: '0px', left: '0px', right: '0px', zIndex: 999999 }} />
                    <div className=" mmt-container mmt-container--full">
                        <div class="mmt-app">
                            <div style={{ position: 'absolute', width: '100%', height: '100%', top: '0px', left: '0px', backgroundColor: item.color_background, opacity: 1, color: 'rgb(255, 255, 255)', pointerEvents: 'none', zIndex: -1 }} />
                            <span className="mmt-menu__item" style={{ display: 'flex' }}><div className="mt-tooltip">
                                <span className="mmt-button call" style={{ backgroundColor: "yellow" }}>
                                    <img alt="url" style={{ height: '30px', marginRight: '20px' }} src={`../../public/images/${item.icon}`} className="mmt-button__icon" />
                                    <span className="mmt-button__label" style={{ color: item.color_text }}>{item.name_button}</span></span>

                            </div></span>
                        </div></div></div>
            )

            ) : <div>Danh sách nút trống</div>} */}

        </>
    )

} export default WelcomeBack